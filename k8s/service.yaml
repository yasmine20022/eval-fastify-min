apiVersion: v1
kind: Service
metadata:
  name: eval-fastify-min
  labels:
    app: eval-fastify-min
spec:
  type: NodePort
  selector:
    app: eval-fastify-min
  ports:
  - port: 80
    targetPort: 8000
