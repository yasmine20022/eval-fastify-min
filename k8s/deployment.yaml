apiVersion: apps/v1
kind: Deployment
metadata:
  name: eval-fastify-min
  labels:
    app.kubernetes.io/name: eval-fastify-min
    app.kubernetes.io/instance: eval-fastify-min
    app.kubernetes.io/component: server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: eval-fastify-min
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  template:
    metadata:
      labels:
        app: eval-fastify-min
        app.kubernetes.io/name: eval-fastify-min
        app.kubernetes.io/instance: eval-fastify-min
        app.kubernetes.io/component: server
    spec:
      containers:
      - name: eval-fastify-min
        image: eval-fastify-min:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8000
        resources:
          requests:
            cpu: 250m
            memory: 256Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 10
          failureThreshold: 3
        startupProbe:
          httpGet:
            path: /health
            port: 8000
          periodSeconds: 10
          failureThreshold: 30
